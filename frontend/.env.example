# Backend API Configuration
VITE_API_BASE_URL=http://localhost:3000

# Cardano Network Configuration
VITE_CARDANO_NETWORK=preprod

# App Configuration (optional)
VITE_APP_URL=http://localhost:5173

# ============================================================
# SERVER-SIDE SECRETS (Configure in Netlify Dashboard, NOT here)
# ============================================================
# These are NOT prefixed with VITE_ and are only used by Netlify Functions.
# ALL of these are REQUIRED - no defaults are provided:
#
#   - MINT_API_URL (e.g., https://your-deno-project.deno.dev/mint)
#   - TRANSFER_API_URL (e.g., https://your-deno-project.deno.dev/transfer)
#   - BLOCKFROST_KEY
#   - SECRET_SEED (minting wallet seed phrase)
#   - CBOR_HEX (Plutus script bytecode)
#   - PROCESSOR_WALLET_ADDRESS (recipient address for token transfers)
#
# Set these in: Netlify Dashboard > Site Settings > Environment Variables
